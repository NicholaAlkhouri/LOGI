@model LOGI.Models.TeamMember

@{
    ViewBag.Title = "Edit Team Member";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Edit Team Member @using (Html.BeginForm("Delete", "AdminTeam", FormMethod.Post, new { id = "deleteForm", onsubmit = "setFormSubmitting()" }))
            {
                <input type="submit" onclick="return confirm('are you sure you want to delete this member?')" class="btn btn-danger" value="Delete" />
                @Html.HiddenFor(m=> m.ID)
            }
        </h1>
        
    </div>
</div>
@using (Html.BeginForm("Edit", "AdminTeam", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "setFormSubmitting()" }))
{
    <div class="row">
        <div class="col-lg-8">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Member Information - English
                </div>
                <div class="panel-body">
                    <div class="form-group">
                        @if (Model.ImageURL != null && Model.ImageURL != "")
                        {
                            <img src="@Model.ImageURL" /><br />
                        }
                        <label for="Images" class="control-label">Image:</label>
                        <input type="file" id="Images" name="Images">
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.FullName)
                        @Html.TextBoxFor(m => m.FullName, new { @class = "form-control", required = "required" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Expertise)
                        @Html.TextBoxFor(m => m.Expertise, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.CurrentRole)
                        @Html.TextBoxFor(m => m.CurrentRole, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Career)
                        @Html.TextBoxFor(m => m.Career, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Education)
                        @Html.TextBoxFor(m => m.Education, new { @class = "form-control" })
                    </div>

                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    Member Information - Arabic
                </div>
                <div class="panel-body">
                    <div class="form-group">
                        @Html.LabelFor(m => m.FullNameAr)
                        @Html.TextBoxFor(m => m.FullNameAr, new { @class = "form-control", required = "required ar-input" })
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.ExpertiseAr)
                        @Html.TextBoxFor(m => m.ExpertiseAr, new { @class = "form-control ar-input" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.CurrentRoleAr)
                        @Html.TextBoxFor(m => m.CurrentRoleAr, new { @class = "form-control ar-input" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.CareerAr)
                        @Html.TextBoxFor(m => m.CareerAr, new { @class = "form-control ar-input" })
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.EducationAr)
                        @Html.TextBoxFor(m => m.EducationAr, new { @class = "form-control ar-input" })
                    </div>
                </div>
            </div>
            <div class="panel panel-default">
                <div class="panel-heading">
                    Member Contact Information
                </div>
                <div class="panel-body">
                    <div class="form-group">
                        @Html.LabelFor(m => m.Email)
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Twitter)
                        @Html.TextBoxFor(m => m.Twitter, new { @class = "form-control" })
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(m => m.Linkedin)
                        @Html.TextBoxFor(m => m.Linkedin, new { @class = "form-control" })
                    </div>
                </div>
            </div>


        </div>

        <div class="col-lg-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Publish
                </div>
                <div class="panel-body">
                    <div class="form-group">
                        <div class="checkbox">
                            <label>
                                @Html.EditorFor(m => m.IsOnline, new { @class = "form-control" })
                                @Html.DisplayNameFor(m => m.IsOnline)
                            </label>
                        </div>
                    </div>

                    <div class="form-group">
                        <div>
                            <label>
                                Type
                            </label>
                            <select class="form-control" name="Type" id="Type" required>
                                <option value="cofounder" @(Model.Type=="cofounder"?"selected":"")>CO-FOUNDER</option>
                                <option value="advisory" @(Model.Type == "advisory" ? "selected" : "")>ADVISORY</option>
                                <option value="team" @(Model.Type == "team" ? "selected" : "")>TEAM</option>
                            </select>
                        </div>
                    </div>

                    <input type="submit" class="btn btn-primary" value="Save Changes" />

                </div>
            </div>

        </div>
    </div>
}



@section scripts {
    <!--Rich Text Editor -->
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
    <script>


        var formSubmitting = true;
        var setFormSubmitting = function () { formSubmitting = true; };

        window.onload = function () {
            window.addEventListener("beforeunload", function (e) {
                var confirmationMessage = 'It looks like you have been editing something. ';
                confirmationMessage += 'If you leave before saving, your changes will be lost.';

                if (formSubmitting) {
                    return undefined;
                }

                (e || window.event).returnValue = confirmationMessage; //Gecko + IE
                return confirmationMessage; //Gecko + Webkit, Safari, Chrome etc.
            });

            $("input").change(function () {
                formSubmitting = false;
            });
        };
    </script>
}
